<div class="container text-center">
  <div class="row">
    <div class="col-12">
      <div class="wrapper">
        <div class="banner-half" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://source.unsplash.com/1600x900/?fitness);">
         <h2>Hi, <%= current_user.first_name.capitalize %>!</h2>
          <%= cl_image_tag current_user.user_image, class: "round_image", id: "image-top" %>
        </div>
        <p><%= current_user.bio %></p>
        <%= link_to "Edit Profile", edit_user_registration_path(current_user), class: "btn home-btn" %>
      </div>
    <%# current_user.bookings.each do |booking| %>
      <div class="card mx-auto" style="width: 400px;">
        <div class="card-body">
          <h5 class="card-title">This is title of session</h5>
          <p class="card-text">
            <li>
                <%#= booking.date %><%#=booking.time%>
              This should be session details
            </li>
          </p>
          <%= link_to "Edit this session", edit_user_session_path(current_user), class: "btn btn-2" %>
          <%= link_to "Delete this session", "", class: "btn btn-2" %>
        </div>
      </div>
    <%# end %>
  </div>
  <div class="bottom-buttons">
    <p><%= link_to "Home", root_path, class:"btn btn-secondary mb-4" %></p>
  </div>
</div>
</div>




<!-- <div class="container text-center">
  <h1>Coach Dashboard</h1>
  <h1>This is Coach dashboard. It should allow coach to create, edit, destroy sessions.</h1>


    <div class="flex_box">
          <%#= cl_image_tag current_user.user_image, class: "round_image" %>
          <h2><%= current_user.first_name %> <%= current_user.last_name %></h2>
          <p><%= current_user.bio %></p>
          <div class="row">
            <div class="flex-box">
              <%# current_user.coach_sessions.each do |coach_session| %>
                <div class="card border-info mb-3" style="width: 18rem;">
                  <div class="card-body">
                    <p class="card-text">
                      <li>
                          <%#= coach_session.duration %> hours of Yoga
                      </li>
                    </p>
                    <%#= link_to "Edit this session", edit_user_session_path(current_user), class: "btn btn-primary m-3" %>
                    <%#= link_to "Delete this session", edit_user_session_path(current_user), method: :delete, class: "btn btn-primary" %>
                  </div>
                </div>
              <%# end %>
            </div>

            </div>

    </div>


</div>
 -->
