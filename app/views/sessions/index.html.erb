<!--   this is the profile that of the coach as seen by the athlete who is browsing  -->
<div class="container text-center">
  <div class="row">
    <div class="col-12">
      <div class="wrapper">
        <div class="banner-half" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://source.unsplash.com/1600x900/?fitness);">
         <h2><%= @coach.first_name.capitalize %></h2>
          <%= cl_image_tag @coach.user_image, class: "round_image", id: "image-top" %>
        </div>
        <p><%= @coach.bio %></p>
        <% bookings = Booking.all.select do |booking| %>
        <% booking.session.coach == @coach %>
        <% end %>
        <p class="m-2">This coach has been booked <strong><%= bookings.count %></strong> Times
          <% if bookings.count <= 0   %>
            <%= "💩" %>
          <% elsif bookings.count >= 1 %>
            <%= "🥉" %>
          <% elsif bookings.count > 3 %>
            <%= "🥈" %>
          <% elsif bookings.count > 5 %>
            <%= "🥇" %>
          <% end %>
        </p>
      </div>
  <div class="container d-flex mx-auto justify-content-center">
    <% @coach.coach_sessions.each do |session| %>
      <div class="col-4 d-flex mx-auto justify-content-center my-2">
        <div class="card mx-auto" style="width: 400px;">
            <div class="card-body">
              <h4 class="card-title"><%= session.title %></h4>
                    <p><%= session.description %><p>
              <%= link_to "Book this session", new_user_booking_path(@coach), class: "btn home-btn" %>
            </div>
          </div>
      </div>
    <% end %>
  </div>
  </div>
  <div class="bottom-buttons">
<!--     <p><%#= link_to "Book Coach", "", class: "btn btn-2"  %></p> -->
    <p><%= link_to "Back to Results", users_path, class:"btn btn-secondary mb-4" %></p>
  </div>
</div>
</div>


